- name: "[BASE APPS]     | Installing useful applications"
  dnf:
    name: ['vim', 'htop', 'pandoc']

- name: "[BASE PKGS]     | Installing platform dependent necessary packages"
  dnf: name="{{item}}"
  with_items:
  - libselinux-python
